import 'package:flutter/material.dart';

class AppConstants {
  static const Color primaryColor = Color(0xFF50BD00);
  static const Color backgroundColor = Color(0xFFF8F9FA);
  static const Color textColor = Color(0xFF212529);
  static const Color secondaryTextColor = Color(0xFF6C757D);
  static const Color cardColor = Color(0xFFFFFFFF);
  static const Color correctAnswerColor = Color(0xFF28A745);
  static const Color wrongAnswerColor = Color(0xFFDC3545);
  static const Color roadColor = Color(0xFF495057);
  static const Color laneLineColor = Color(0xFFFFFFFF);
  
  static const double defaultPadding = 16.0;
  static const double borderRadius = 12.0;
  static const double carSize = 60.0;
  static const double roadHeight = 200.0;
  static const double laneWidth = 120.0;
  
  static const Duration animationDuration = Duration(milliseconds: 300);
  static const Duration splashDuration = Duration(seconds: 3);
  static const Duration questionDisplayDuration = Duration(seconds: 8);
}